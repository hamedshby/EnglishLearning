# Day 3 Lesson Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add a complete third English lesson about presenting a project and its achievements.

**Architecture:** Create one static HTML lesson fragment that follows the existing `days/day-2.html` structure and is loaded by the existing menu and navigation code in `script.js`. Keep the current CSS classes and client-side behavior unchanged.

**Tech Stack:** HTML5, existing CSS classes, existing vanilla JavaScript lesson loader

## Global Constraints

- The lesson topic is «معرفی پروژه و دستاوردها».
- Keep the visual structure, Persian tone, English level, and approximate content volume aligned with days 1 and 2.
- Do not add `<audio>` elements or references to `day-3-word.mp3` or `day-3.mp3`.
- Show navigation back to day 2 and no navigation to day 4.
- Do not refactor the lesson loader or convert lessons to a data-driven system.
- Preserve the user's unrelated `README.md` modification.

---

### Task 1: Add the day 3 lesson fragment

**Files:**
- Create: `days/day-3.html`
- Reference: `days/day-2.html`

**Interfaces:**
- Consumes: the existing `data-file="days/day-3.html"` menu item in `index.html`, CSS classes from `style.css`, and delegated handlers in `script.js`
- Produces: a loadable HTML fragment containing `#completeLessonButton` and a single `[data-navigation-id="2"]` button

- [ ] **Step 1: Add a failing structural verification**

Run this PowerShell check before creating the file:

```powershell
$lesson = Get-Content -Raw -Encoding UTF8 'days/day-3.html'
if (-not $lesson.Contains('روز سوم')) { throw 'Missing day label' }
```

Expected: FAIL because `days/day-3.html` does not exist.

- [ ] **Step 2: Create the lesson fragment**

Create `days/day-3.html` using the established section markup from `days/day-2.html`. Use these exact content requirements:

```text
Hero:
  روز سوم
  معرفی پروژه و دستاوردها
  ۳۰ دقیقه | سطح مقدماتی | Project & Achievements

Vocabulary (English — Persian):
  project — پروژه
  achievement — دستاورد
  challenge — چالش
  outcome — نتیجه
  contribute — مشارکت کردن
  reduce — کاهش دادن
  increase — افزایش دادن
  efficiency — بهره‌وری
  latency — تأخیر
  impact — تأثیر

Practical sentences:
  I worked on a payment processing project.
  The goal of the project was to improve reliability.
  I was responsible for designing and developing the backend services.
  One of the main challenges was high API latency.
  We optimized the database queries and added caching.
  As a result, we reduced the average response time by 40 percent.

Grammar:
  Use the simple past for completed project actions.
  Include positive, negative, and question forms with project examples.

Interview questions:
  Can you describe a project you are proud of?
  What was the biggest technical challenge in that project?
  What was the result of your work?

Speaking framework:
  Project → Role → Challenge → Action → Result

Quiz:
  Four questions covering achievement, simple past, measurable results,
  and the correct Project/Role/Challenge/Action/Result order.

Final exercise:
  Describe a project you are proud of.

Navigation:
  One previous button with data-navigation-id="2"
  Label: روز دوم: توضیح تجربه کاری و مسئولیت‌ها
```

Use the existing section classes and IDs verbatim, including:

```html
<button id="completeLessonButton" class="complete-lesson-button" type="button">
    <span>○</span>
    علامت‌گذاری درس به‌عنوان تکمیل‌شده
</button>
```

- [ ] **Step 3: Verify structural and content requirements**

Run:

```powershell
$lesson = Get-Content -Raw -Encoding UTF8 'days/day-3.html'
$required = @(
  'روز سوم',
  'معرفی پروژه و دستاوردها',
  'واژگان جدید',
  'جملات کاربردی',
  'نکته گرامری امروز',
  'سؤال‌های مصاحبه',
  'آزمون کوتاه',
  'تمرین امروز',
  'completeLessonButton',
  'data-navigation-id="2"'
)
foreach ($value in $required) {
  if (-not $lesson.Contains($value)) { throw "Missing required content: $value" }
}
if ($lesson -match '<audio|day-3.*\.mp3|data-navigation-id="4"') {
  throw 'Day 3 contains forbidden audio or day 4 navigation'
}
if ([regex]::Matches($lesson, 'class="english-word"').Count -ne 10) {
  throw 'Expected exactly 10 vocabulary entries'
}
if ([regex]::Matches($lesson, 'Interview Question').Count -ne 3) {
  throw 'Expected exactly 3 interview questions'
}
if ([regex]::Matches($lesson, '<span class="question-label">Question \d').Count -ne 4) {
  throw 'Expected exactly 4 quiz questions'
}
```

Expected: PASS with no output.

- [ ] **Step 4: Commit the lesson**

```powershell
git add -- 'days/day-3.html'
git commit -m 'feat: add day 3 project lesson'
```

### Task 2: Verify integration with the existing application

**Files:**
- Verify: `index.html`
- Verify: `script.js`
- Verify: `days/day-3.html`

**Interfaces:**
- Consumes: menu metadata and the existing `loadLesson`/delegated navigation behavior
- Produces: a day 3 lesson reachable by both the sidebar and day 2's next button

- [ ] **Step 1: Run static integration assertions**

Run:

```powershell
$index = Get-Content -Raw -Encoding UTF8 'index.html'
$day2 = Get-Content -Raw -Encoding UTF8 'days/day-2.html'
$day3 = Get-Content -Raw -Encoding UTF8 'days/day-3.html'
if (-not $index.Contains('data-file="days/day-3.html"')) {
  throw 'Sidebar does not target day 3'
}
if (-not $day2.Contains('data-navigation-id="3"')) {
  throw 'Day 2 does not navigate to day 3'
}
if (-not $day3.Contains('data-navigation-id="2"')) {
  throw 'Day 3 does not navigate back to day 2'
}
```

Expected: PASS with no output.

- [ ] **Step 2: Serve the site and inspect day 3**

Run:

```powershell
python -m http.server 8000
```

Open `http://localhost:8000/?day=3` and verify:

```text
- The lesson loads without a visible error.
- Persian and English text render correctly.
- All lesson sections match the visual styling of day 2.
- No audio player is visible.
- The completion button changes state when clicked.
- The previous button opens day 2.
- Reloading ?day=3 keeps day 3 selected.
```

- [ ] **Step 3: Run repository checks**

Run:

```powershell
git diff --check HEAD^ HEAD
git status --short
```

Expected: no whitespace errors; only the pre-existing `README.md` modification remains unstaged.
